#include "character.h"
#include <stdlib.h>
#include <stdio.h>

void create_character(character_t *hero)
{
    for(uint8_t i=0U; i<10U; i++)
    {
        printf("i = %d\n",(uint8_t)(rand() % 20));
    }



}
